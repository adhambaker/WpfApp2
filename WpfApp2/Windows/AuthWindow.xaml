<Window x:Class="WpfApp2.AuthWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login / Register"  WindowStartupLocation="CenterScreen" Width="293" Height="214">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Username" />
            <TextBox x:Name="TxtUsername" Margin="0,6,0,0"/>

            <TextBlock Text="Password" Margin="0,8,0,0"/>
            <PasswordBox x:Name="PwdPassword" Margin="0,6,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,12,0,0">
            <Button Content="Login As Student" Width="100" Click="LoginStudent_Click"/>
            <Button Content="Login As Teacher" Width="100" Margin="30,0,0,0" Click="TeacherLogin_Click"/>
        </StackPanel>

        
    </Grid>
</Window>
